# Fresh WordPress Blog theme

## Usage

Start WordPress in docker:

```sh
deno task docker
```

Start Fresh website:

```sh
deno task start
```

This will watch the project directory and restart as necessary.

You can access the dashboard of WordPress at http://localhost/wp-admin.
